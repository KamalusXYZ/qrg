{% extends 'base.html.twig' %}

{% block title %}Ajouter un tag{% endblock %}

{% block body %}
<div class="container">
    <div>
    <h2>Liste des Tags existants</h2>
    <ul>
        {# {% for tagExisting in tags %}
            <li>{{ tagExisting }}</li>
        {% endfor %} #}
        {% for tag in tags %}
            <li style="font-weight:bold;">#{{tag.name|upper}}</li>
        {% endfor %}
    </ul>
        
    </div>

        <h1>Ajouter un nouveau tag</h1>
            {{ include('tag/_form.html.twig') }}
    <a href="{{ path('app_tag_index') }}">back to list</a>
</div>


{% endblock %}
